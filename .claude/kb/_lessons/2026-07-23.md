# Ledger de promoção — 2026-07-23

Registro do que o `/learn` promoveu. Append-only.

## Promovido

| Entrada | Camada/Domínio | `promoted_from` | Candidatas absorvidas |
|---------|----------------|-----------------|------------------------|
| `prova-de-verificacao` | `operations` / `verificacao` (novo) | `CICLO_DE_VIDA`, `DOWNLOADER_LOCAL` | 5 (C1, C2, D1, D2, D3) |

Caminho: `.claude/kb/operations/verificacao/runbook/prova-de-verificacao.md`
Plano: [`2026-07-23-plan.md`](2026-07-23-plan.md)

## Backup

**Nenhum** — a entrada e o domínio `verificacao` são novos; nada foi sobrescrito. O passo de backup
do `/learn` (`.claude/.cache/kb-backup/`) não se aplica a criação.

## Verificação

- `Test-LessonProvenance` — sem `lesson-without-source`
- `kb-lint` — entrada válida, dentro do orçamento de `runbook`
- `/sync-context` — `_index.yaml` passa a listar `verificacao`

## Não promovido

As 4 lições `[pontual]` do acervo (bug de empacotamento, flags implícitas do yt-dlp, sonda de
bitrate, emenda datada do DEFINE) — one-off por construção, seguem só no `archive/`.
