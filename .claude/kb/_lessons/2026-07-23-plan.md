# Plano de promoção — 2026-07-23

> Gerado por `/learn`. **Plano, não ação.** Nada foi escrito na KB até aprovação explícita.

## Gatilho

`Test-LessonsReady` → `Ready=True`, **5 candidatas pendentes** / limiar 3, de **2 features**
(`CICLO_DE_VIDA`, `DOWNLOADER_LOCAL`). Nenhuma promovida antes (`promoted_from` vazio em toda a KB).

## Candidatas no acervo

| # | Feature | Lição `[candidata]` |
|---|---------|---------------------|
| C1 | CICLO_DE_VIDA | Verificação: *verde sem contrafactual não prova nada* |
| C2 | CICLO_DE_VIDA | Ferramentas: *o artefato de verificação tem de ser reconstruído antes de medir* |
| D1 | DOWNLOADER_LOCAL | *Verificar o próprio comentário antes de confiar nele* |
| D2 | DOWNLOADER_LOCAL | *Teste que afirma o comportamento antigo falha em silêncio quando a decisão muda* |
| D3 | DOWNLOADER_LOCAL | *Critério sem linha de evidência é critério não verificado* |

## Detecção de recorrência

Todas as 5 são a **mesma prática**, em duas modalidades de falha — e **cada modalidade tem suporte
nas duas features**, que é o critério de recorrência (≥2 features):

| Modalidade | CICLO_DE_VIDA | DOWNLOADER_LOCAL |
|------------|---------------|------------------|
| **A prova não é falsificável** — o teste passaria mesmo se a defesa não existisse | C1: três defesas "cobertas" que a mutação não matava | D1: comentário afirmando que o M4A copiava a trilha; a medição mostrou recodificação para 8,7 MB |
| **A evidência é velha ou não existe** — mede-se o passado, ou não se mede | C2: `.exe` obsoleto em `dist/` concordava com código velho e mascarava 2 testes de contrato | D2: BUILD_REPORT exibindo "24 passed" de rodada anterior com a suíte vermelha há 4 commits · D3: SC-4/SC-5 chegaram ao `/ship` sem linha de evidência |

**Não** é recorrência inventada: são 5 ocorrências independentes, em 2 ciclos, com evidência medida
registrada em cada `SHIPPED`.

## Proposta — 1 entrada

| Campo | Valor |
|-------|-------|
| Caminho | `.claude/kb/operations/verificacao/runbook/prova-de-verificacao.md` |
| `layer` | `operations` |
| `domain` | `verificacao` (**novo** — a KB só tinha `media`) |
| `content_type` | `runbook` |
| `promoted_from` | `[CICLO_DE_VIDA, DOWNLOADER_LOCAL]` |

**Rationale.** A prática não é de mídia — é de **como este projeto aceita que algo está verificado**.
Pô-la em `tools/media` a esconderia do único momento em que importa (qualquer gate de qualquer
feature). Domínio novo `verificacao` na camada `operations`, que é onde mora "como eu rodo/valido".

**Por que 1 entrada e não 2.** As duas modalidades são o mesmo erro visto de ângulos diferentes —
tratar uma alegação como se fosse uma medição. Separá-las produziria duas entradas que se citam
mutuamente em toda leitura.

## O que NÃO é promovido

As 4 lições `[pontual]` do acervo seguem no arquivo, sem promoção — one-off por construção
(bug de empacotamento, flags do yt-dlp, sonda de bitrate, emenda do DEFINE).

## Verificação prevista pós-aplicação

- `Test-LessonProvenance` → sem `lesson-without-source`
- `kb-lint` → entrada nova válida (frontmatter + orçamento de tamanho)
- `/sync-context` → `_index.yaml` passa a listar `verificacao`
